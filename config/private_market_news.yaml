private_market_news:

  settings:
    lookback_hours: 24
    dedupe_days: 1
    max_candidates_per_company: 40
    min_score: 0.55
    max_story_age_hours: 48  # Only stories from last 2 days

  companies:

    - name: "Cerebras Systems"
      limit: 5
      enabled: true

    - name: "Groq"
      limit: 5
      enabled: true

    - name: "SambaNova Systems"
      limit: 5
      enabled: true

    - name: "Liquid Death"
      limit: 5
      enabled: true

    - name: "Neuralink"
      limit: 5
      enabled: true

    - name: "Automation Anywhere"
      limit: 3
      enabled: true

    - name: "Dialpad"
      limit: 3
      enabled: true

    - name: "Impossible Foods"
      limit: 3
      enabled: true

    - name: "Dataminr"
      limit: 3
      enabled: true

    - name: "BitPay"
      limit: 3
      enabled: true

    - name: "Mythic AI"
      limit: 3
      enabled: true
